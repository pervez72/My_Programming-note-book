#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,m;
    cin>>n>>m;
    if ((n>=1 && n<=pow(10,5)) &&(m>=1 && m<=pow(10,5)))
    {
        int a[n];
        for (int i = 0; i < n; i++)

    
        
    }
}